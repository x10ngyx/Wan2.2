# Quick Start Document

- Added `quick_start.md` for bootstrapping a fresh remote machine.
- The document records the intended workflow: clone code from GitHub, download the packed runtime from OSS, unpack it under `/hy-tmp/env/Wan2.2`, set cache environment variables, place model weights under `/hy-tmp/models/Wan2.2-T2V-A14B`, validate GPU/code/model files, and push code changes back to `x10ngyx/main`.
- No cache implementation or experiment runner code was changed.
