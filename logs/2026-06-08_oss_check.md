# 2026-06-08 OSS CLI Check

- User provided HyCloud OSS usage examples for logging in, listing `oss://datasets/`, and downloading data into `/hy-tmp/`.
- Read `PROGRESS.md` and checked git status at session start.
- Verified `oss` is installed at `/usr/local/bin/oss`.
- Ran `oss help`, `oss version`, `oss help cp`, and `oss help ls`.
- Confirmed version `v1.2.36+prod` and that later weight downloads should use `/hy-tmp` as the local destination.
- Relevant command patterns:
  - `oss login`
  - `oss ls -s -d oss://datasets/`
  - `oss cp oss://datasets/<object> /hy-tmp/`
  - `oss cp oss://<bucket>/<prefix> /hy-tmp/<target> -r`
